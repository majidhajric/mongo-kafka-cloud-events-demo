spring:

  data:
    mongodb:
      uri: mongodb://localhost:27017/test?replicaSet=rs0;uuidRepresentation=STANDARD;
      host: localhost
      database: test

  kafka:
    bootstrap-servers: localhost:9092
    events-topic: events-topic
